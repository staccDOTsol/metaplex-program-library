/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet';
import * as web3 from '@solana/web3.js';
import * as beetSolana from '@metaplex-foundation/beet-solana';

/**
 * Arguments used to create {@link UserState}
 * @category Accounts
 * @category generated
 */
export type UserStateArgs = {
  open: boolean;
  empty: boolean;
  rewards: boolean;
  fees: boolean;
};

export const userStateDiscriminator = [72, 177, 85, 249, 76, 167, 186, 126];
/**
 * Holds the data for the {@link UserState} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class UserState implements UserStateArgs {
  private constructor(
    readonly open: boolean,
    readonly empty: boolean,
    readonly rewards: boolean,
    readonly fees: boolean,
  ) {}

  /**
   * Creates a {@link UserState} instance from the provided args.
   */
  static fromArgs(args: UserStateArgs) {
    return new UserState(args.open, args.empty, args.rewards, args.fees);
  }

  /**
   * Deserializes the {@link UserState} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(accountInfo: web3.AccountInfo<Buffer>, offset = 0): [UserState, number] {
    return UserState.deserialize(accountInfo.data, offset);
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link UserState} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig,
  ): Promise<UserState> {
    const accountInfo = await connection.getAccountInfo(address, commitmentOrConfig);
    if (accountInfo == null) {
      throw new Error(`Unable to find UserState account at ${address}`);
    }
    return UserState.fromAccountInfo(accountInfo, 0)[0];
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey('91TwXG4wTqJSm6GU8yn2AfBaTqvRi8XPurpkHHNVU7z4'),
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, userStateBeet);
  }

  /**
   * Deserializes the {@link UserState} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [UserState, number] {
    return userStateBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link UserState} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return userStateBeet.serialize({
      accountDiscriminator: userStateDiscriminator,
      ...this,
    });
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link UserState}
   */
  static get byteSize() {
    return userStateBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link UserState} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(UserState.byteSize, commitment);
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link UserState} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === UserState.byteSize;
  }

  /**
   * Returns a readable version of {@link UserState} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      open: this.open,
      empty: this.empty,
      rewards: this.rewards,
      fees: this.fees,
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const userStateBeet = new beet.BeetStruct<
  UserState,
  UserStateArgs & {
    accountDiscriminator: number[] /* size: 8 */;
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['open', beet.bool],
    ['empty', beet.bool],
    ['rewards', beet.bool],
    ['fees', beet.bool],
  ],
  UserState.fromArgs,
  'UserState',
);
